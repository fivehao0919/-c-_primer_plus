#include <iostream>
#include <vector>
#include <array>
#include <string>

int main()
{
    const int size=10;
    std::vector<std::string> vec(size);
    std::array<std::string, size> arr;
    return 0;
}
